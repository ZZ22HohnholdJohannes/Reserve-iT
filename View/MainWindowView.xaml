<UserControl x:Class="Reserve_iT.View.MainWindowView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             DataContext="{Binding RelativeSource={RelativeSource AncestorType={x:Type Window}}, Path=DataContext}">
    <Grid>
        <!-- Dein Layout -->
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="*"/>
            <ColumnDefinition Width="Auto"/>
        </Grid.ColumnDefinitions>

        <Image Source="pack://application:,,,/Images/Logo_Dark.png" Height="100" Width="100"
               HorizontalAlignment="Right" VerticalAlignment="Top"
               Stretch="Uniform"
               Margin="10" Grid.Row="0" Grid.Column="1"/>

        <Grid Grid.Row="1" Grid.Column="0" Grid.ColumnSpan="2"
              HorizontalAlignment="Center" VerticalAlignment="Center">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="Auto"/>
                <ColumnDefinition Width="*" />
                <ColumnDefinition Width="Auto"/>
            </Grid.ColumnDefinitions>

            <Button Content="Zimmer buchen" 
        Command="{Binding DataContext.ShowBookingViewCommand, RelativeSource={RelativeSource AncestorType={x:Type Window}}}" 
        Width="200" Height="80" Margin="20"/>

            <Button Content="Bewertungen" Width="200" Height="80" Margin="20" Grid.Column="2"/>
        </Grid>

        <Button Content="Buchung stornieren" Width="150" Height="40"
                HorizontalAlignment="Center" VerticalAlignment="Center"
                Grid.Row="2" Grid.ColumnSpan="2" Margin="5"/>

        <StackPanel Orientation="Horizontal" HorizontalAlignment="Center"
                    Grid.Row="3" Grid.ColumnSpan="2" Margin="10">
            <TextBlock Text="Administrator" VerticalAlignment="Center" Margin="5"/>
            <TextBox Width="200" Margin="5"/>
            <Button Content="Login" Width="100" Margin="5"/>
        </StackPanel>
    </Grid>
</UserControl>
